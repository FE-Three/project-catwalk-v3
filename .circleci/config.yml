version: 2.1
jobs:
  build:
    docker:
      - image: cimg/ruby:2.7.1-node



    steps:
      - checkout
      - run: cd my-app && npm install --save
      - run: cd my-app && npm run test
      - store_test_results:
          path: test-results

Workflows:
  test:
    jobs:
      - build