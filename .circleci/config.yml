version: 2.1
jobs:
  build:
    docker:
      - image: cimg/ruby:2.7.1-node



    steps:
      - checkout
      - run: ls
      - run: cd my-app
      - run: ls
      - run: npm install
      - run: npm test

Workflows:
  test:
    jobs:
      - buildcd ls